/**
 * Re-exports for onboarding step handler types.
 *
 * Provides convenient imports for step implementations
 * without reaching into the engine module directly.
 */

export type {
  OnboardingStepHandler,
  StepDefaults,
  StepExecutionContext,
  StepResult,
} from "../engine";
